{% for year, articles in collections.articlesByYear %}
<h4>{{ year }}</h4>
<ul>
  {% for article in articles %}
  <li>
    <a href="{{ article.url }}">{{ article.data.title }}</a>
  </li>
  {% endfor %}
</ul>
{% endfor %}
