---
layout: layouts/home.njk
eleventyNavigation:
  key: Projects
  order: 3
---

<h2>⛏️ Projects</h2>

<div>
  Here are all the fun side projects I have worked on, and for each one I have
  documented how I built the project, technology choices, and how it turned out.
</div>

<h3>⚡ Active</h3>

<div class="w-full grid grid-cols-1 gap-4 md:grid-cols-2">
  {% for project in collections["project-active"] | reverse %}
  <a href="{{ project.url }}">
    <div
      class="relative h-24 border-solid border-2 border-gray-500 border-opacity-100 rounded-md cursor-pointer hover:shadow-xl"
    >
      <div
        class="absolute w-full h-full {{
          project.data.pattern
        }} pattern-blue-500 pattern-bg-white 
      pattern-size-6 pattern-opacity-20 rounded-md"
      ></div>
      <div class="absolute flex items-center w-full h-full">
        <div
          class="mx-auto bg-slate-100 py-1 px-4 rounded-md dark:bg-slate-600"
        >
          {{ project.data.title }}
        </div>
      </div>
    </div>
  </a>
  {% endfor %}
</div>

<h3>🗃️ Archived / Lost</h3>

<div class="w-full grid grid-cols-1 gap-4 md:grid-cols-2">
  {% for project in collections["project-archived"] | reverse %}
  <a href="{{ project.url }}">
    <div
      class="relative h-24 border-solid border-2 border-gray-500 border-opacity-100 rounded-md cursor-pointer hover:shadow-xl"
    >
      <div
        class="absolute w-full h-full {{
          project.data.pattern
        }} pattern-blue-500 pattern-bg-white 
      pattern-size-6 pattern-opacity-20 rounded-md"
      ></div>
      <div class="absolute flex items-center w-full h-full">
        <div
          class="mx-auto bg-slate-100 py-1 px-4 rounded-md dark:bg-slate-600"
        >
          {{ project.data.title }}
        </div>
      </div>
    </div>
  </a>
  {% endfor %}
</div>
